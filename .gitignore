# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules/
/.pnp
.pnp.js

# testing
/coverage

# production
/build
dist/

# database (SQLite 로컬 파일 — 경로는 DATABASE_URL로 주입)
*.db
*.db-journal

# 음원 원본 (루트) — 사용본은 client/src/assets/audio/에 커밋됨
/*.mp3

# 아바타 업로드 (로컬 개발 산출물 — 경로는 UPLOADS_DIR로 주입)
uploads/

# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
