# ── korean_tales 클라이언트 환경변수 예시 ───────────────────────
# 사용법: 이 파일을 client/.env로 복사한 뒤 값을 채운다

# 게임 서버 주소 (REST + Socket.io) — 미설정 시 http://localhost:4000
# Render 배포 서버를 쓰면: https://korean-tales-server.onrender.com
# VITE_SERVER_URL=http://localhost:4000
